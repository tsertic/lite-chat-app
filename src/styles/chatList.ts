const styles = (theme: any) => {
  return {
    root: {
      width: '300px',
      height: '90vh',
      border: '1px solid gray',
      borderRadius: '4px',
      boxShadow: '1px 1px 2px rgba(0,0,0,.3)'
    }
  };
};

export default styles;
